#include <stdint.h>

#include "mbed_assert.h"

namespace mbed
{

class I2C
{

public:
    //virtual int write(const void* buffer, size_t size) = 0;
    //virtual int bar(const void* buffer, size_t size) = 0;
};

} // namespace mbed